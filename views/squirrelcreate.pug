extends layout
block content
h1= title
form(id='squirrelForm', action="/resource/squirrels", method="POST")
div.form-group
label.squirrelAttr(for='squirrel_color') squirrel_color
input#squirrel_color(type='text', name='squirrel_color')
div.form-group
label.squirrelAttr(for='squirrel_breed') squirrel squirrel_breed
input#squirrel_breed(type='text', name='squirrel_breed')
div.form-group
label.squirrelAttr(for='squirrel_price') squirrel_price
input#cost(type='number', name='squirrel_price')
input(type="submit")
script.
document.forms['squirrelForm'].addEventListener('submit', (event) => {
event.preventDefault();
// TODO do something here to show user that form is being submitted
fetch(event.target.action, {
method: 'POST',
body: new URLSearchParams(new FormData(event.target)) // event.target is the form
}).then((resp) => {
return resp.json(); // or resp.text() or whatever the server sends
}).then((body) => {
alert("Save succeded")
// TODO handle body
}).catch((error) => {
// TODO handle error
alert("Save failed " + error )
});
});